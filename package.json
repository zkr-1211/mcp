{
  "name": "postar-pipe-mcp",
  "version": "1.0.5",
  "description": "Unified CI/CD MCP Server - orchestrates GitLab and Jenkins MCPs with dynamic skills",
  "type": "module",
  "main": "bin/postar-pipe-mcp.js",
  "bin": {
    "postar-pipe-mcp": "bin/postar-pipe-mcp.js"
  },
  "files": [
    "bin",
    "release"
  ],
  "scripts": {
    "build": "vite build && npm run copy:skills",
    "build:tsc": "tsc && npm run copy:skills",
    "copy:skills": "cp -r src/skills dist/",
    "dev": "vite",
    "start": "node dist/server.js",
    "prepare": "npm run build",
    "pkg:build": "npm run build && pkg . -t node24-macos-arm64 -o release/postar-pipe-mcp-macos-arm64 --build",
    "release": "npm version patch && git push && git push --tags",
    "release:minor": "npm version minor && git push && git push --tags",
    "release:major": "npm version major && git push && git push --tags"
  },
  "keywords": [
    "mcp",
    "ci",
    "cd",
    "gitlab",
    "jenkins"
  ],
  "author": "zhangkr",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "js-yaml": "^4.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vite-plugin-node": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pkg": {
    "scripts": [
      "dist/**/*.js"
    ],
    "assets": [
      "dist/skills/**/*"
    ],
    "targets": [
      "node24-macos-arm64",
      "node24-macos-x64",
      "node24-linux-x64",
      "node24-win-x64"
    ],
    "outputPath": "release"
  }
}
